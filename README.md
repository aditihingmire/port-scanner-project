# port-scanner-project
A port scanner is a networking tool used to discover open communication channels ("ports") on a target device or server.
How it Works:
It uses Python's socket library to systematically attempt connections to specific ports on a target IP address.
If the connection succeeds, the port is open.
If the connection fails or times out, the port is closed.
